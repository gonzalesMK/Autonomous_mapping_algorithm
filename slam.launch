<launch>
  <master auto="start"/>
  <param name="/use_sim_time" value="true" />  

  <!-- Run the map server -->
  <node name="SLAM" pkg="gmapping" type="slam_gmapping" args="scan:=scan _odom_frame:=AMR _base_frame:=AMR _map_frame:=world"/>
  
</launch>
